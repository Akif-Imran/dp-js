export const funkyNoDoc = (target, wordBank) => {
  return 0;
};

/**
 * A function that return way to generate target string.
 * @param {string} target
 * @param {string[]} wordBank the array of string to use to generate the target string.
 * @returns {number} number of ways to generate target string.
 */
export const funkyDoc = (target, wordBank) => {
  return 0;
};
